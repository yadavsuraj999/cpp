#include <iostream>

using namespace std;

int main()
{

    // unary start

    // for add one

    // int a = 55;

    // cout << a++ << endl;
    // cout << "add one = " << a;

    // for minus one

    //  int a = 55;

    // cout << a-- << endl;
    // cout << "minus one = " << a;

    // unary end

    // logical start

    // && opereter

    // int a = 60, b = 80;

    // cout <<(a > b && b < a);

    // int a = 60, b = 80;

    // cout <<(a < b && b > a);

    // oror operater

    // int a = 500, b = 900;
    // cout << (a >= b or b >= a);
}