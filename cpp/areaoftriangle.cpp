#include <iostream>

using namespace std;

int main()
{
    float half = 0.5, base, height, area;

    cout << " enter base = ";
    cin >> base;
    cout << " enter height = ";
    cin >> height;


    area = 0.5 * base * height;

    cout << " your area is = " << area;
}